services:
  - type: web
    name: pcb-defect-detector
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
    plan: free
    autoDeploy: true
